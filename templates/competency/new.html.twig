{% extends 'base.html.twig' %}

{% block title %}New Competency{% endblock %}

{% block body %}
    <div class="mx-auto max-w-2xl space-y-6">
        <div>
            <h1 class="text-2xl font-bold tracking-tight text-slate-900">Add competency</h1>
            <p class="mt-1 text-sm text-slate-500">Create a group (e.g. Gallup, IP121) and list its items as name &rarr; description.</p>
        </div>
        <div class="card p-6 sm:p-8">
            {{ include('competency/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
